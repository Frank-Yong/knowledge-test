# Session 2026-03-06 15:33:46

## Summary
- Changed cache document loading to surface IO/JSON failures with a sanitized error instead of silently returning an empty list.

## Rationale
- Swallowing parse errors made it impossible to distinguish “no documents” from “cache unreadable.” Now callers get a clear failure with only the cache filename exposed.

## Changes
- `FileKnowledgeRepository.LoadDocumentsAsync` now throws `InvalidOperationException` with a sanitized message on IO/JSON errors; successful loads still filter out null/empty paths.

## Validation
- Not run (no automated tests executed for this change).

## Status
- Cache read errors now propagate; consider handling this upstream (e.g., surfacing in status or triggering cache repair).
