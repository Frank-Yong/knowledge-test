# OperatingContextMcp Session Log

- Timestamp: 2026-03-04 05:32:35 (local)
- Source repo: operating-context-mcp
- Branch: phase-2-mcp-handshake
- Trigger: reviewer JSON-RPC version compliance comment + `Log it`

## Change implemented

### Enforced `jsonrpc: "2.0"` validation in request pipeline
- File: `src/OperatingContextMcp.Server/Worker.cs`
- Added validation before method dispatch:
  - `jsonrpc` property must exist
  - value kind must be `String`
  - value must be exactly `"2.0"`
- When invalid and `id` is present, server now returns `-32600 Invalid Request`.
- Invalid/no-id messages continue to be ignored as notification-style traffic.

### Regression test added
- File: `tests/OperatingContextMcp.Tests.Unit/WorkerProtocolTests.cs`
- Added: `ExecuteAsync_InvalidJsonRpcVersionWithId_ReturnsInvalidRequest`
- Verifies request `{"jsonrpc":"1.0","id":15,"method":"ping"}` returns:
  - same request id
  - error code `-32600`
  - message `Invalid Request`

## Why this matters
- Aligns request handling with JSON-RPC 2.0 protocol contract.
- Prevents malformed-version requests from being processed as valid.
- Produces deterministic client-visible failures for easier debugging.

## Validation
- Command: `dotnet test tests/OperatingContextMcp.Tests.Unit/OperatingContextMcp.Tests.Unit.csproj`
- Result: passed (`32` total, `0` failed).

## Status snapshot
- Reviewer comment on missing `jsonrpc` version validation is addressed with runtime check + unit coverage.
