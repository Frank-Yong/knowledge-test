# OperatingContextMcp Session Log

- Timestamp: 2026-03-04 05:16:56 (local)
- Source repo: operating-context-mcp
- Branch: phase-2-mcp-handshake
- Trigger: reviewer performance note on `DiscardUntilHeaderTerminatorAsync` allocation churn + `Log it`

## Change implemented

### Removed per-byte allocations in header terminator drain loop
- File: `src/OperatingContextMcp.Server/Worker.cs`
- In `DiscardUntilHeaderTerminatorAsync`:
  - replaced `Queue<byte>` rolling window + `ToArray()` allocations with an allocation-free state machine matcher for `\r\n\r\n`
  - retained existing read chunking and max examined byte bound behavior

## Why this matters
- Eliminates per-byte allocations in malformed-header drain path.
- Reduces potential GC churn and CPU overhead under hostile or malformed input.
- Keeps behavior equivalent for detecting header terminator and returning control.

## Validation
- Command: `dotnet test tests/OperatingContextMcp.Tests.Unit/OperatingContextMcp.Tests.Unit.csproj`
- Result: passed (`31` total, `0` failed).

## Status snapshot
- Reviewer comment on `Queue<byte>` + `ToArray()` allocation overhead is addressed with fixed-state matching.
