# Session 2026-03-04 08:21:09

## Summary
- Made `UpsertDocumentsAsync` resilient to null or duplicate paths by skipping empty paths and using last-write-wins dictionary insertion.

## Rationale
- Previous change left `existing.ToDictionary` susceptible to null paths or duplicate case-insensitive keys, causing upsert to throw on malformed cache entries.

## Changes
- Updated `FileKnowledgeRepository` upsert logic to build the path map manually, ignoring null/empty paths and allowing last-write-wins for duplicates.

## Validation
- Not run (no automated tests executed for this change).

## Status
- Cache upsert no longer fails on malformed or duplicate path entries; broader cache validation tests still pending.
