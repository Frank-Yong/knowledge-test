# Session 2026-03-04 08:19:04

## Summary
- Hardened cache listing to tolerate malformed documents with null/empty paths.
- Added guard in `ListAsync` and sanitized deserialized documents to prevent NREs when filtering by path prefix.

## Rationale
- `document.Path.StartsWith(...)` could throw if cache JSON contained null paths; we now drop invalid entries and guard the filter.

## Changes
- Updated `FileKnowledgeRepository` to filter out documents with null/empty `Path` after deserialization and guard path-prefix filtering.

## Validation
- Not run (no automated tests executed in this change).

## Status
- Cache listing resilient to malformed path entries; further validation tests still pending.
