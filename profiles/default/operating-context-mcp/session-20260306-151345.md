# Session 2026-03-06 15:13:45

## Summary
- Added null filtering in `LoadDocumentsAsync` to handle cache arrays that contain null elements without throwing.

## Rationale
- System.Text.Json can deserialize null array elements into `List<KnowledgeDocument>`; a null entry would NRE during path filtering. We now skip null documents before checking paths.

## Changes
- Updated `FileKnowledgeRepository.LoadDocumentsAsync` to filter `document is not null && !string.IsNullOrWhiteSpace(document.Path)`.

## Validation
- Not run (no automated tests executed for this change).

## Status
- Cache load resilient to null array entries; consider adding tests for malformed cache files.
