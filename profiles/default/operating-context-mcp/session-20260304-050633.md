# OperatingContextMcp Session Log

- Timestamp: 2026-03-04 05:06:33 (local)
- Source repo: operating-context-mcp
- Branch: phase-2-mcp-handshake
- Trigger: reviewer note on `tools/list` advertising unimplemented tools + `log it`

## Change implemented

### Discovery now reflects executable tools only
- File: `src/OperatingContextMcp.Server/Hosting/RequestDispatcher.cs`
- Added `CanDispatch(string toolName)` method to expose whether a tool has a registered handler.

- File: `src/OperatingContextMcp.Server/Worker.cs`
- Updated `CreateToolsListResult()` to filter tool discovery output:
  - skip tools that cannot be dispatched (`if (!dispatcher.CanDispatch(tool.Name)) continue;`)
- Result: `tools/list` now advertises only currently executable tools.

### Test coverage update
- File: `tests/OperatingContextMcp.Tests.Unit/WorkerProtocolTests.cs`
- Strengthened `ExecuteAsync_ToolsList_ReturnsRegisteredToolDefinitions`:
  - still asserts `knowledge_get` and `knowledge_status` are present
  - now asserts unimplemented tools are not advertised (`knowledge_search`, `knowledge_refresh`)

## Why this matters
- Keeps MCP discovery accurate and prevents clients from seeing tools that always fail at runtime.
- Aligns advertised capability surface with `RequestDispatcher` execution surface.

## Validation
- Command: `dotnet test tests/OperatingContextMcp.Tests.Unit/OperatingContextMcp.Tests.Unit.csproj`
- Result: passed (`31` total, `0` failed).

## Status snapshot
- Reviewer comment on tools discovery/runtime mismatch is addressed.
